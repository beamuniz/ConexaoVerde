@model ConexaoVerde.Web.Models.UsuarioModel

@{
    ViewData["Title"] = "Esqueceu Senha";
}

<section class="bg-white dark:bg-gray-900">
    <div class="flex justify-center min-h-screen">
        <div class="hidden bg-cover lg:block lg:w-2/5" style="background-image: url('https://img.freepik.com/free-photo/natural-cosmetic-product-collection_23-2148574896.jpg?t=st=1730999797~exp=1731003397~hmac=f7bc3c6e2e719e6563099bbc2f92097672da642ac6bb07be523ca64cbd8f6876&w=740');">
        </div>
        <div class="flex items-center w-full max-w-3xl p-8 mx-auto lg:px-12 lg:w-3/5">
            <div class="w-full">
                <h1 class="text-2xl font-semibold tracking-wider text-[#2C2F33] capitalize dark:text-white">
                    Esqueceu sua Senha
                </h1>
                <p class="mt-4 text-[#2C2F33] dark:text-gray-400">
                    Informe o seu e-mail para receber as instruções de recuperação de senha.
                </p>

                @if (ViewBag.ErrorMessage != null)
                {
                    <div class="text-red-500 mt-2">@ViewBag.ErrorMessage</div>
                }

                @if (ViewBag.Sucess != null)
                {
                    <div class="mt-2 text-green-500 bg-green-100 p-2 rounded-md">
                        @ViewBag.Sucess
                    </div>
                }

                <form asp-action="EsqueceuSenha" method="POST" class="grid grid-cols-1 gap-6 mt-8">
                    <div>
                        <label for="Email" class="block mb-2 text-sm text-[#2C2F33] dark:text-gray-200">E-mail</label>
                        <input asp-for="Email" type="email" class="block w-full px-5 py-3 mt-2 text-black placeholder:text-black bg-white border border-gray-200 rounded-md dark:placeholder-gray-600 dark:bg-gray-900 dark:text-gray-300 dark:border-gray-700 focus:border-[#98FF98] dark:focus:border-[#98FF98] focus:ring-[#98FF98] focus:outline-none focus:ring focus:ring-opacity-40"/>
                        <span asp-validation-for="Email" class="text-red-500 text-sm"></span>
                    </div>

                    <button type="submit" class="mt-3 w-full px-6 py-3 text-black bg-[#98FF98] rounded-md focus:outline-none hover:bg-[#66cc66]">
                        Enviar Instruções
                    </button>
                </form>

                <div class="mt-4 text-center">
                    <a href="Login" class="text-sm text-[#98FF98] hover:text-[#66cc66] dark:text-[#98FF98] dark:hover:text-[#66cc66]">
                        Voltar para o Login
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
